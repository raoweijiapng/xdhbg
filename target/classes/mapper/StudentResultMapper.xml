<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xdh.dao.StudentResultDao">

    <select id="selectStudentNameById" resultType="cn.xdh.entity.Student">
        select username,id
        from xdh_student
        where id = ${id}
    </select>

    <select id="selectPaperAndTest" resultType="cn.xdh.entity.PaperAndTest">
        select *
               from xdh_test_paper  p,xdh_attend_test  t where p.id = t.test_id and t.student_id=${id}
    </select>

    <select id="selectPaperToList" resultType="cn.xdh.entity.PaperAndTest">
        select *
        from xdh_test_paper  p,xdh_attend_test  t where  p.id = ${id} and t.student_id = ${student_id}
    </select>

    <select id="selectPaperToLookname" resultType="cn.xdh.entity.PaperAndTest">
        select *
        from xdh_test_paper  p,xdh_attend_test  t where p.id = t.test_id and t.student_id= ${id}
                                                    and p.name like CONCAT('%',#{lookname},'%')
    </select>
    
</mapper>